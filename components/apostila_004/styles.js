import {stylesSheet} from 'reat-native';

const styles = stylesSheet.create({
    container: {
        flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    }
}) 

export default styles;
